// components/BoxNode.jsx
const BoxNode = ({ data }) => {
  return (
    <div
      className="absolute border-2 border-dashed border-slate-400 rounded-xl bg-slate-50 z-[-1]"
      style={{
        width: data.width,
        height: data.height,
        padding: "8px",
        boxSizing: "border-box",
      }}
    >
      <div className="font-semibold text-slate-600 mb-1 text-sm">
        📦 {data.label}
      </div>
    </div>
  );
};

export default BoxNode;
