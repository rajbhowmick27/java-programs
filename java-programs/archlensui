const { fitView } = useReactFlow();

  useEffect(() => {
    // Delay fitView slightly to allow rendering
    const timeout = setTimeout(() => {
      fitView({ padding: 0.2 });
    }, 100);
    return () => clearTimeout(timeout);
  }, [nodes, edges, fitView]);
